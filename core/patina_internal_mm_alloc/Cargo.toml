[package]
name = "patina_internal_mm_alloc"
version.workspace = true
repository.workspace = true
license.workspace = true
edition.workspace = true
readme = "README.md"
description = "Shared pool allocator and page allocator trait for MM cores (supervisor and user)."

[lints]
workspace = true

[dependencies]
log = { workspace = true }
spin = { workspace = true }
